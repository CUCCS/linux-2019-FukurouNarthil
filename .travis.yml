sudo: required

language: sh

before_install:
    - sudo apt-get update -q
    - export scversion="stable" # or "v0.4.7", or "latest"
    - mkdir workspace
    - cd workspace
    - wget "https://storage.googleapis.com/shellcheck/shellcheck-${scversion}.linux.x86_64.tar.xz"
    - tar --xz -xvf shellcheck-"${scversion}".linux.x86_64.tar.xz
    - sudo cp shellcheck-"${scversion}"/shellcheck /usr/bin/
    - /usr/bin/shellcheck --version
    - sudo apt install p7zip-full
    - wget http://sec.cuc.edu.cn/huangwei/course/LinuxSysAdmin/exp/chap0x04/web_log.tsv.7z
    - 7z x -oexp0x04/shell web_log.tsv.7z
    - cd exp0x04/shell

script:
  - bash ../../test.sh
  - bash ../images/pic.sh -h
  - bash ../images/pic.sh -c
  - bash ../images/pic.sh -C
  - bash ../images/pic.sh -w xxx
  - bash ../images/pic.sh -p www
  - bash ../images/pic.sh -s yyy
  - bash ../images/pic.sh -t

  - bash player.sh
  - bash webanalyze.sh h
  - bash webanalyze.sh 1
  - bash webanalyze.sh 2
  - bash webanalyze.sh 3
  - bash webanalyze.sh 4
  - bash webanalyze.sh 5
  - bash webanalyze.sh /shuttle

after_script:
  - /usr/bin/shellcheck test.sh

after_failure:
  - id
  - pwd
  - df -h
  - cat error.log

notifications:
  email:
    on_success: never
